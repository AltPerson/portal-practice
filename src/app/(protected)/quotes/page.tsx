import { Box } from '@mui/material';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Quotes',
};

export default function QuotesPage() {
  return <Box>Quotes page</Box>;
}
